# Peça A (extração + interface)
google-genai
groq
anthropic       # provedor Claude (LLM_PROVIDER=claude)
python-docx
pydantic
streamlit
matplotlib  # apenas para o gerar_relatorio_pai.py

# Peça B (robô)
pyautogui
pywinauto
opencv-python  # necessario para pyautogui.locateOnScreen(confidence=...)

# O boletim do testamento (PDF) NAO precisa de pacote nenhum: e' escrito a mao
# em Python puro (ver peca_a_extracao/boletim.py). Assim nao ha 'pip install'
# para falhar PC a PC.

# Para ler .doc antigo (Word 97-2003), é preciso UM destes no sistema:
#  - LibreOffice (recomendado): instalar a app, traz o 'soffice'
#  - Git for Windows: traz antiword embutido
# Não são pacotes pip; instalam-se ao nível do sistema.
